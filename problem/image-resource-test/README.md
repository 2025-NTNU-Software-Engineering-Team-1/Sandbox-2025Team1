# Image Resource Test

## Description
This problem tests the "Resource Data" feature.
The system will provide an image file named `image.ppm` in the working directory.
Your program should read this image (P3 format PPM) and output its width and height.

## Input Format
No input from stdin.

## Output Format
Two integers separated by a space: `width height`.
